'use strict';

describe('Service: generateexcelFactory.js', function () {

    // load the service's module
    beforeEach(module('studymonitorApp'));

    // instantiate service
    var generateexcelFactory;
    beforeEach(inject(function (_generateexcelFactory_) {
        generateexcelFactory = _generateexcelFactory_;
    }));
});
